python mysql연결
>cmd에서 직접설치
>console

연결정보로 쿼리 실행 객체를 만들어야한다

drop table users
create table if not exists users(
	id int auto_increment primary key,
	name varchar(255) not null,
	age int not null,
	email varchar(255) not null,
	create_date timestamp default now()
);
if not exists : table이 없다면 만듦

insert,update,delete 는 실제 db에 변화가 생기기 때문에 commit을 해야한다.

파이썬에서 많이 사용하는 패턴 : bulk insert
create table users(
  id varchar(255) primary key,
  pw varchar(255) not null
);

insert into users(id, pw)
values('hong', '1234');
2번 인서트시 예외 발생하면 처리


크몽, 숨고
크롤링과 파싱

200 정상
300 리다이렉트
400 요청의 잘못
500 서버오류

요청하면 틀만 보내고
ajax로 데이터를 다시 보낸다
리퀘는 요청 한 번이라 
크롤링의 리퀘스트로는 페이지 안의 데이터를 모두는 가져오지 못함

브라우저 자동화도구 (셀레니움)



딥러닝 > 심층학습, 심화학습

threshlod(역치) 아르타
1개의 뉴런 : 퍼셉트론
다중 퍼셉트론(multylayer)
가중치와 합

데이터 스케일링 : 다른 단위나 범위를 가지는 변수들의 범위를 재정의
> 0 ~ 1 사이로 매핑, 0 ~ 100 사이로 매핑 등 여러가지
standardScaler
>feature의 평균을 좌표계의 원점으로 이동시키고 최대 분산을 1로 (반지름이 1인 원 안에 재정렬)

RobustScaler
>평균과 분산대신 중간값(median)과 1/4값?(quartile)을 사용, 이상치에 영향x

MinMaxScaler

